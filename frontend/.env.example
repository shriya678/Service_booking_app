# Copy this file to .env and adjust values as needed.
# Vite ONLY exposes env vars prefixed with VITE_ to the browser.
# Anything without VITE_ stays hidden — important for secrets.

VITE_API_URL=http://localhost:4000
